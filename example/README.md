# example

A Vite + React playground for the [`i-input`](../package) library. It consumes
the library directly from the workspace (`"i-input": "workspace:*"`).

## Develop

From the repo root:

```sh
yarn dev
```

or from this folder:

```sh
yarn workspace example dev
```
